/* eslint-disable import/no-duplicates */
// import { expect } from '@open-wc/testing';
// import { StandaloneAuthenticationManager } from '../../../src/app/env/standalone/StandaloneAuthenticationManager.js';
// import { container } from '../../../src/dews-mobile.js';
//
// import '../../../src/dews-mobile.js';

describe('분리된 어셈블리에서의 Dependency Injection 테스트', () => {
  // it('분리된 어셈블리에서 @injectable', async () => {
  //   // arrange
  //   expect(container).is.not.undefined;
  //   expect(container.isRegistered(StandaloneAuthenticationManager)).is.false;
  //
  //   // act
  //   await dews.app.start();
  //
  //   // assert
  //   expect(container.isRegistered(StandaloneAuthenticationManager)).is.true;
  // });
});
