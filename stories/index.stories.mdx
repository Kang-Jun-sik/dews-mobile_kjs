import { Story, Preview, Meta, Props, withKnobs, withWebComponentsKnobs  } from '@open-wc/demoing-storybook';
import { html } from 'lit-html';
import '../dist/index.js';

<Meta
  title="MDX|Index"
  // withActions('mouseover', 'click .btn') : custom 으로 이벤트 주
  decorators={[withKnobs, withWebComponentsKnobs]}
  parameters={{ component: 'dews-mobile', options: { selectedPanel: 'storybookjs/knobs/panel' } }}
/>

<!-- 아래는 Doc 부분 -->
# Index Demo StoryBook

## Simple
<Preview>
  <Story name="Simple" height="220px">
    {html`
      <dews-mobile></dews-mobile>
    `}
  </Story>
</Preview>

## Custom Title
<Preview>
  <Story name="Set value" height="220px">
    {() => {
      // 다른 모듈 or api 에 있는 데이터 가져와서 테스트 가능할까?
      // const data = `call function`;
      return html`
        <dews-mobile title="Hello World"></dews-mobile>
      `;
    }}
  </Story>
</Preview>

## API

API 문서는 .mdx 에 `<Props of="/component name/">` 으로 등록

<Props of="dews-mobile" />
